/**
 * Loading component.
 *
 * Displays a simple loading message while asynchronous
 * data is being fetched.
 *
 * This component is intentionally minimal and reusable
 * across the application.
 *
 * @returns {JSX.Element} Loading state message
 */
export default function Loading() {
  return <p>Loading…</p>;
}
